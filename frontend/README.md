# 智学工坊前端

## 安装依赖

```powershell
cd frontend
npm install
```

## 启动开发服务

```powershell
npm run dev
```

默认访问地址：

```text
http://127.0.0.1:5173
```

后端地址通过 `.env.development` 的 `VITE_API_BASE_URL` 配置，默认：

```text
http://127.0.0.1:8001
```
